# [画面名] 要件定義

## 1. 画面情報

- 画面名:
- feature 名:
- route:
- 関連画面:
- ステータス:
- 作成日:
- 更新日:

## 2. 目的とスコープ

### 2-1. 目的

- この画面でユーザーに達成してもらうこと
- この画面が業務フローの中で担う役割

### 2-2. スコープ

- この画面で提供する機能
- この画面では扱わない機能

## 3. 対象ユーザーと利用条件

- 想定ユーザー:
- 認証要件:
- 到達経路:

## 4. 前提条件と依存

- 関連 feature:
- 関連 API:
- 前提データ:
- 外部サービス依存:
- レイアウト制約:

## 5. ユーザーストーリー

- ユーザーとして、[目的] のために [操作] したい
- ユーザーとして、[確認したい内容] を [画面上で把握] したい

## 6. 画面概要

### 6-1. 画面構成

- ヘッダー:
- メインコンテンツ:
- 補助領域:
- フッター:
- モーダル / ドロワー:

### 6-2. 主要アクション

- 初期表示時に行うこと:
- ユーザー操作:
- 操作結果:

## 7. ユーザーフロー

1. ユーザーが画面に到達する
2. 初期表示で必要なデータを取得する
3. ユーザーが主要操作を行う
4. 成功時 / 失敗時に画面が状態を更新する
5. 次画面遷移または画面内完結で終了する

## 8. 機能要件

### 8-1. 初期表示

- 初期表示時に必要な API / データ取得
- 初期表示時の UI
- 権限不足 / 未認証時の扱い

### 8-2. 入力 / 操作

- 入力項目:
- 操作可能なボタン / リンク:
- 活性 / 非活性条件:
- 二重送信防止:

### 8-3. 出力 / 表示

- 一覧 / 詳細 / 集計の表示内容
- ラベルや補助文言
- 日付、通貨、件数などの表示形式

## 9. 状態要件

- Loading:
  - どのタイミングで表示するか
  - 何を操作不能にするか
- Empty:
  - データ 0 件時の表示
  - 次に促すアクション
- Success:
  - 成功メッセージ
  - 画面遷移の有無
- Validation Error:
  - 入力エラー表示位置
  - メッセージ方針
- API Error:
  - 表示方式
  - 再試行導線
- Unauthorized:
  - `401` 時の遷移先
  - セッション破棄の要否

## 10. バリデーションと業務ルール

- 必須項目:
- フォーマット制約:
- 相関チェック:
- 業務上の入力制約:
- 操作可能条件:

## 11. API / データ要件

### 11-1. 利用 API 一覧

| 用途         | Method | Endpoint   | 入力  | 出力  | 備考           |
| ------------ | ------ | ---------- | ----- | ----- | -------------- |
| 例: 一覧取得 | GET    | `/example` | query | items | 初期表示で利用 |

### 11-2. データ要件

- 画面表示に必要なデータ項目
- フロントで加工が必要な項目
- 欠損時のフォールバック方針

## 12. UI / UX 要件

- モバイル優先で成立すること
- 600px / 960px での表示差分
- 主要 CTA を画面内で見失わないこと
- 長文、長い一覧、0 件時の読みやすさを担保すること

## 13. アクセシビリティ要件

- ラベルは常時表示する
- キーボードだけで主要操作を完結できる
- フォーカス順が自然である
- エラーメッセージは入力直下かつ読み上げ可能にする
- コントラストは WCAG AA を満たす

## 14. 非機能要件

- 想定表示件数:
- パフォーマンス上の注意:
- 監視 / ログ出力の要件:
- セキュリティ上の注意:

## 15. 対象外

- この画面では実装しないこと
- 将来拡張として扱うこと

## 16. Definition of Done

- 主要ユーザーフローが成立する
- Loading / Empty / Success / Error を設計済みである
- 認証 / 権限制御を考慮している
- API 契約と表示項目の対応が明確である
- テスト観点が洗い出されている

## 17. 未解決事項

- 要確認事項
- バックエンド確認待ち
- デザイン確認待ち
